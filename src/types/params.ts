export interface SearchParam {
    depature: number
    stop: number
    dest: number
    unix: number
    head: number
    ahead: number
}

export interface BusStopLabel {
    value: string
    label: string
}